server {
    listen 80;
    listen [::]:80;
    server_name example.com ipv4.example.com ipv6.example.com;

    root /var/www/v4v6;
    index index.php index.html;

    add_header X-Content-Type-Options "nosniff" always;
    add_header Referrer-Policy "no-referrer-when-downgrade" always;
    add_header Permissions-Policy "geolocation=(), microphone=(), camera=()" always;

    error_page 404 /404.html;

    location = /api/check {
        rewrite ^ /api/check.php last;
    }

    location = /api/ip {
        rewrite ^ /api/ip.php last;
    }

    location = /api/ipv4 {
        rewrite ^ /api/ipv4.php last;
    }

    location = /api/ipv6 {
        rewrite ^ /api/ipv6.php last;
    }

    location = /api/probes {
        rewrite ^ /api/probes.php last;
    }

    location / {
        try_files $uri $uri/ /index.php$is_args$args;
    }

    location ~ \.php$ {
        include fastcgi_params;
        fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
        fastcgi_param SCRIPT_NAME $fastcgi_script_name;
        fastcgi_pass unix:/run/php/php8.2-fpm.sock;
    }

    location ~ /\. {
        deny all;
    }
}

# HTTPS deployment example:
# server {
#     listen 443 ssl http2;
#     listen [::]:443 ssl http2;
#     server_name example.com ipv4.example.com ipv6.example.com;
#
#     ssl_certificate /etc/letsencrypt/live/example.com/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/example.com/privkey.pem;
#
#     root /var/www/v4v6;
#     index index.php index.html;
#
#     add_header X-Content-Type-Options "nosniff" always;
#     add_header Referrer-Policy "no-referrer-when-downgrade" always;
#     add_header Permissions-Policy "geolocation=(), microphone=(), camera=()" always;
#
#     error_page 404 /404.html;
#
#     location = /api/check { rewrite ^ /api/check.php last; }
#     location = /api/ip { rewrite ^ /api/ip.php last; }
#     location = /api/ipv4 { rewrite ^ /api/ipv4.php last; }
#     location = /api/ipv6 { rewrite ^ /api/ipv6.php last; }
#     location = /api/probes { rewrite ^ /api/probes.php last; }
#
#     location / { try_files $uri $uri/ /index.php$is_args$args; }
#
#     location ~ \.php$ {
#         include fastcgi_params;
#         fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
#         fastcgi_param SCRIPT_NAME $fastcgi_script_name;
#         fastcgi_pass unix:/run/php/php8.2-fpm.sock;
#     }
#
#     location ~ /\. { deny all; }
# }
